# 公式与变量问题批注处理条目

对应修订文档：`output/doc/genai_employment_exposure_revise_公式变量修订版.docx`

说明：以下仅整理与“公式、变量、指标定义、模型解释”直接相关的导师批注及处理情况；摘要措辞、图文衔接等非公式类问题未纳入主表。表中“批注具体内容”尽量保留原批注原话。

| 序号 | 批注具体内容 | 对应上下文 | 处理方式 | 修订结果 |
|---:|---|---|---|---|
| 1 | 解释 GDI_c（是否对应上述城市维度？）与 Exposure_g（是否对应上述岗位维度？） | 理论框架中 `GDI_c × Exposure_g` 首次出现处 | 在公式 (1) 后补充两个变量的定义 | 明确 `GDI_c` 为城市 `c` 的 GenAI 显性招聘需求强度，`Exposure_g` 为岗位族 `g` 的 LLM 暴露度 |
| 2 | 解释 Z( ) | `Shock_cg = Z(GDI_c × Exposure_g)` | 补充标准化含义 | 写明 `Z(·)` 表示在主模型估计样本内进行 Z-score 标准化，即 `Z(X)=(X-X̄)/sd(X)` |
| 3 | 写出对应原假设。1. 交互冲击对指数无显著影响；... | H1、H2a、H2b、H3 假设段落 | 新增原假设段 | 补充 `H1_0`、`H2a_0`、`H2b_0`、`H3_0`，分别对应 SRI、技能熵、技能类别占比和异质性机制 |
| 4 | 增加这两个指标的解释 or 此处简写成“模型分析指标”，后文具体解释。 | 技能 token 清洗后提到“技能熵和 SRI_cg 指数”处 | 明确指标名称 | 改为“技能熵 `H_cg` 和技能结构重塑指数 `SRI_cg`”，避免提前出现未解释缩写 |
| 5 | M_id | 七维 LLM 文本信号评分中 `M` 的定义 | 统一变量下标 | 将命中信号组数量记为 `M_id`，表示岗位 `i` 在维度 `d` 上命中的信号组数量 |
| 6 | 解释 clamp01( )，M_id | `Score_id = clamp01(M_id / 2.5)` | 补充函数和变量解释 | 写明 `M_id∈{0,1,2,3}`，并定义 `clamp01(x)=min(max(x,0),1)` |
| 7 | 解释每个变量 | `I_i`、`R_i`、`A_i`、`T_i` 合成公式 | 补充七维变量说明 | 新增 `ln、tk、dc、sr、rt、hj、po` 的中文含义及归一化口径 |
| 8 | 记为 GenAI_i | 城市 GenAI 需求强度定义段 | 在正文中明确岗位层变量名 | 将显性 GenAI 标记写为 `GenAI_i` |
| 9 | NaiveSignal_i | `GenAI_i = 1(NativeSignal_i > 0)` 附近 | 统一信号变量名称 | 正文解释为 `NativeSignal_i`，对应岗位 `i` 的 LLM 原生应用信号得分 |
| 10 | 即 GenAI_i = 1，否则为 0 | 显性 GenAI 岗位定义 | 补充分段逻辑 | 写明若 `NativeSignal_i > 0`，则 `GenAI_i=1`；否则 `GenAI_i=0` |
| 11 | 记为 GDI_c | 城市层 GenAI 需求强度定义 | 明确城市层变量名 | 将城市层 GenAI 需求强度记为 `GDI_c` |
| 12 | N_c | `GDI_c` 公式后分母解释 | 修正分母变量 | 将“其中，N 为城市 c 的岗位总数”改为“其中，`N_c` 为城市 `c` 的岗位总数” |
| 13 | GDI_c | `GDI_c` 公式解释段 | 统一变量称呼 | 后文解释中继续使用 `GDI_c`，避免只写中文名称导致符号断裂 |
| 14 | LLMScore，how to quantify? | 岗位族 LLM 暴露度 `Exposure_g` 定义前后 | 补充岗位层综合暴露分来源 | 明确 `T_i` 记为 `LLMScore_i`，数据输出中 `llm_exposure_total_i = 100 × T_i` |
| 15 | 记号乱码？ | `Exposure_g=mean(LLMScore_i∨i∈g)` | 替换乱码符号 | 改为 `Exposure_g = (1/N_g) Σ_{i∈g} LLMScore_i` |
| 16 | Summation over c and g? n or N? What’s n_cg and n_c? | 城市结构加权暴露度 `Exposure_c` | 补充求和范围和计数变量定义 | 改为 `Exposure_c = Σ_{g∈G} share_cg × Exposure_g`，并解释 `n_cg`、`n_c`、`share_cg` |
| 17 | H_cg | 技能熵定义标题和正文 | 明确技能熵符号 | 将技能熵记为 `H_cg` |
| 18 | N_cgs | 技能熵中技能数量定义 | 补充三维计数变量 | 写明 `N_cgs` 表示城市 `c`、岗位族 `g` 中包含技能 `s` 的岗位数量 |
| 19 | P_cgs | 技能占比定义 | 统一占比变量 | 写明 `p_cgs` 表示技能 `s` 在城市 `c`、岗位族 `g` 单元中的占比 |
| 20 | Summation over? | `p_cgs` 与 `H_cg` 公式 | 补充求和范围 | `p_cgs` 和 `H_cg` 公式均加入 `s∈S_cg` 的求和范围 |
| 21 | 因变量 | 技能结构重塑指数定义 | 明确模型角色 | 将 `SRI_cg` 明确表述为本文核心因变量 |
| 22 | What’s v? C? D? | `Share_cg,v^raw = C_cg,v / D_cg` | 补充变量解释 | 写明 `v∈{GenAI, Digital, HighOrder, Routine}`，`C_cg,v` 为第 `v` 类技能 token 计数，`D_cg` 为全部有效技能 token 总数 |
| 23 | Prior? | 平滑公式 `prior_g,v` | 补充先验定义 | 写明 `prior_g,v` 为岗位族 `g` 在全样本中第 `v` 类技能的平均占比 |
| 24 | 本文取 | 平滑参数 `λ` | 明确参数取值 | 写明本文主口径取 `λ=10` |
| 25 | 与 Share_cg 的关系 | `SRI_cg^smooth` 合成公式 | 统一平滑 share 口径 | 将 SRI 合成式改为由 `GenAIShare_cg^smooth`、`DigitalShare_cg^smooth`、`HighOrderShare_cg^smooth`、`RoutineShare_cg^smooth` 构成 |
| 26 | 乘积？是否存在 GDI 大但 Exposure 小，或反过来，如果存在，如何区分？ | 核心解释变量 `Shock_cg` 段落 | 补充交互项经济解释 | 写明一高一低时乘积项相对有限，只有城市需求和岗位暴露同时较高时才体现较强交互冲击 |
| 27 | 图7没有包括低 GDI_c 城市？ | 图 7 热力图解释 | 调整图文推断来源 | 将“由图 7 直接推出”改为“结合表 5 与附表中的城市分组结果可见” |
| 28 | Mu_c 与 nv_g 是否 0-1 变量？ | 双向固定效应模型 `Y_cg = β_1 Shock_cg + μ_c + ν_g + ε_cg` | 补充固定效应解释 | 写明 `μ_c` 和 `ν_g` 不是单一 0-1 变量，而分别对应一组城市虚拟变量和一组岗位族虚拟变量的固定效应参数 |
| 29 | What’s A1-A4? | 表 7 基准回归表头 | 替换模型列名 | 将 `A1`、`A2`、`A3`、`A4` 改为“基础模型”“城市FE”“岗位族FE”“双向FE” |
| 30 | GDIz × Exposurez 与主变量的关系 | 表 11 稳健性检验 | 补充替代解释变量说明 | 将写法统一为 `GDI_c^z × Exposure_g^z`，并说明其表示先分别标准化后再相乘 |