# NCSS 全量数据核验与交付说明 - 2026-04-16

这份说明用于回答三个问题：

1. 当前全国 NCSS 主数据是否已经有效跑通
2. 当前仓库是否已经整理到可以作为项目主线提交
3. 还存在哪些已知但无法通过继续重跑解决的边界

## 1. 当前主数据状态

- 全国详情主种子：`data/input/ncss/ncss_detail_urls_all_areas.csv`
- 主种子规模：`41407` 个唯一职位
- 主 manifest：`data/raw/ncss/manifests/ncss_detail_manifest_all_areas.jsonl`
- 主 manifest 当前行数：`41407`
- 全量详情原始解析表：`data/raw/ncss/records/ncss_jobs_all_areas_raw.jsonl`
- 当前有效详情数：`41400`
- 清洗后主数据：`data/processed/ncss/ncss_jobs_all_areas_clean.csv`
- 当前清洗后职位数：`30985`

这意味着当前项目已经拥有一份可以直接进入技能抽取、正文分析和招聘要求建模的全国 JD 主数据。

## 2. 为什么不是 41407 条都进入 raw

当前差额不是脚本卡住，而是源站页面本身不可恢复：

- `4` 条为源站已删除页面，页面标题为“信息已删除”
- `3` 条页面虽然仍可访问，但正文节点为空，无法提取真实 JD

当前确认的异常职位 ID 如下。

### 已删除页面

- `Bcgc6ZuroyviAnpkkPNHRe`
- `KW6HD8Uk3Aj2ozZxyUCD6r`
- `HfXpSWvTwu5BsZ654VSS1f`
- `2kz3xM23Qo6GMWgFTwLhGx`

### 正文为空页面

- `EniUVds3WUhHx1dJMnU3up`
- `FD1pzwPYoZgKPouPFwhBct`
- `DURdjbLJBwTdZU6ZQ5jWrW`

因此，当前 `41407 -> 41400` 的差额属于源站边界，不再建议继续对全量 `41407` 页面做低效重跑。

## 3. 当前数据是否足够作为项目主体

结论：可以。

推荐口径如下：

- 用 `data/processed/ncss/ncss_jobs_all_areas_clean.csv` 作为项目主体 JD 数据
- 用 `data/processed/ncss/ncss_listings_all_areas_flat.csv` 作为全国职位覆盖底表和总样本口径
- 在论文或报告中说明样本形成链路：`41407` 个全国唯一职位种子 -> `41400` 条有效详情 -> `30985` 条清洗后主样本

如果需要强调保守性，可以补一句：剩余 `7` 条并非解析失败，而是源站已删除或正文为空。

## 4. 当前仓库整理结果

本次已完成以下整理：

- `docs/` 只保留文本型项目文档
- 论文 PDF 和下载记录统一归入 `papers/pdfs/` 与 `papers/download_results.csv`
- `src/` 按 `platforms/` 与 `tools/` 重组，不再把各平台脚本混放在根层
- 删除了临时 refetch 种子与临时 refetch manifest
- 删除了 Python 缓存目录 `src/__pycache__/`
- 更新了根目录、`docs/`、`papers/`、`data/`、`src/` 下相关 README
- 把 `51job` 社招的进度快照、cursor 和调度日志明确划为本地运行态；数据快照则可按阶段提交给组员分析

## 5. 提交建议

当前状态可以提交，但建议提交说明里明确三点：

- 主数据文件是 `data/processed/ncss/ncss_jobs_all_areas_clean.csv`
- 全国覆盖口径来自 `data/processed/ncss/ncss_listings_all_areas_flat.csv`
- 仍少的 `7` 条详情是源站异常，不是任务未完成

如果后续还要继续扩充，不建议重跑全量详情抓取；优先沿用当前本地 HTML、主种子和重建 manifest 的流程做增量核验。
